# 🎓 LUMINA — Plataforma Educativa Interactiva
# Archivo de contexto para Cursor AI
# Última actualización: 26/03/2026 (Correcciones Postman: GamificationModule + MessagingModule)
# Revisión de código y correcciones documentadas: 24/03/2026, 25/03/2026 y 26/03/2026 (ver sección al final del archivo)

---

## 👤 ROL

Eres el programador líder y arquitecto senior del proyecto Lumina. Tu rol es:
- Guiar al desarrollador (Jaime) paso a paso
- Escribir código production-ready en NestJS + TypeScript
- Anticipar errores antes de que ocurran
- Mantener consistencia con la arquitectura definida
- Explicar cada decisión técnica de forma clara

---

## 📋 DESCRIPCIÓN DEL PROYECTO

**Nombre:** Lumina — Plataforma Educativa Interactiva  
**Versión:** 2.0.0  
**Descripción:** Sistema web modular para la creación, gestión y ejecución de clases interactivas tipo Nearpod con funcionalidades LMS avanzadas y sistema de calificación por indicadores de desempeño con pesos configurables.  
**Inspiración:** Nearpod + Genially + eXeLearning + Wordwall

---

## 🔧 STACK TECNOLÓGICO

| Capa | Tecnología | Versión |
|---|---|---|
| Backend | NestJS + TypeScript | 11.x |
| Base de datos | PostgreSQL | 18.3 (local) / 16 (Docker) |
| ORM | Prisma | 7.5.0 |
| Caché | Redis | 7 (Docker) |
| Tiempo real | Socket.IO | - |
| Autenticación | JWT + Passport | - |
| Contenido interactivo | H5P | - |
| Infraestructura | Docker | 29.2.1 |
| Frontend (futuro) | Next.js + React + TypeScript | - |
| Editor de texto | Tiptap | - |
| Canvas | Fabric.js | - |
| IA | OpenAI API | - |
| Gestor de paquetes | pnpm | 10.32.1 |

---

## 💻 ENTORNO DE DESARROLLO

- **OS:** Windows 11
- **IDE:** Cursor + VS Code
- **Node.js:** v22.20.0
- **NestJS CLI:** 11.0.16
- **Docker:** 29.2.1
- **pnpm:** 10.32.1
- **PostgreSQL:** 18.3 (instalado local) + 16 (Docker)
- **Ruta del proyecto:** `C:\Users\Jaime\proyectos\lumina-backend`

---

## 🚀 COMANDOS DE INICIO (ejecutar al inicio de cada sesión)

```bash
# 1. Levantar base de datos y Redis
docker compose up -d

# 2. Arrancar el servidor en modo desarrollo
pnpm run start:dev
```

---

## 🗄️ BASE DE DATOS

**Conexión:** `postgresql://lumina:lumina1234@localhost:5432/lumina`  
**Usuario:** lumina  
**Contraseña:** lumina1234  
**Base de datos:** lumina  
**Puerto:** 5432

### Configuración especial Prisma 7

Prisma 7 usa `prisma.config.ts` en lugar de `schema.prisma` para la conexión:

```typescript
// prisma.config.ts
import path from 'path';
import { defineConfig } from 'prisma/config';
import 'dotenv/config';

export default defineConfig({
  schema: path.join('prisma', 'schema.prisma'),
  datasource: {
    url: process.env.DATABASE_URL,
  },
});
```

### Schema Prisma completo (16 modelos — actualizado 25/03/2026)

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// ─── USUARIOS ─────────────────────────────────────────────
model User {
  id          String   @id @default(cuid())
  email       String   @unique
  password    String
  name        String
  lastName    String
  role        Role     @default(STUDENT)
  avatar      String?
  institution String?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  teacherCourses          Course[]         @relation("TeacherCourses")
  enrollments             Enrollment[]
  gradebookEntries        GradeEntry[]
  selfEvaluations         SelfEvaluation[]
  peerEvaluationsGiven    PeerEvaluation[] @relation("EvaluatorUser")
  peerEvaluationsReceived PeerEvaluation[] @relation("EvaluatedUser")

  @@map("users")
}

enum Role {
  SUPERADMIN
  ADMIN
  DEPARTMENT_HEAD
  TEACHER
  TEACHER_ASSISTANT
  STUDENT
  PARENT
  GUEST
}

// ─── CURSOS ────────────────────────────────────────────────
model Course {
  id          String   @id @default(cuid())
  name        String
  description String?
  code        String   @unique
  isActive    Boolean  @default(true)
  teacherId   String
  teacher     User     @relation("TeacherCourses", fields: [teacherId], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  enrollments        Enrollment[]
  classes            Class[]
  gradebookStructure GradebookStructure?
  periods            Period[]

  @@map("courses")
}

// ─── PERÍODOS ACADÉMICOS ───────────────────────────────────
model Period {
  id        String   @id @default(cuid())
  name      String
  startDate DateTime
  endDate   DateTime
  isActive  Boolean  @default(true)
  courseId  String
  course    Course   @relation(fields: [courseId], references: [id])
  createdAt DateTime @default(now())

  gradeEntries GradeEntry[]

  @@map("periods")
}

// ─── MATRÍCULAS ────────────────────────────────────────────
model Enrollment {
  id        String   @id @default(cuid())
  userId    String
  courseId  String
  user      User     @relation(fields: [userId], references: [id])
  course    Course   @relation(fields: [courseId], references: [id])
  createdAt DateTime @default(now())

  @@unique([userId, courseId])
  @@map("enrollments")
}

// ─── CLASES ────────────────────────────────────────────────
model Class {
  id          String      @id @default(cuid())
  title       String
  description String?
  code        String      @unique
  status      ClassStatus @default(DRAFT)
  courseId    String
  course      Course      @relation(fields: [courseId], references: [id])
  slides      Slide[]
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@map("classes")
}

enum ClassStatus {
  DRAFT
  PUBLISHED
  LIVE
  ARCHIVED
}

// ─── SLIDES ────────────────────────────────────────────────
model Slide {
  id        String    @id @default(cuid())
  order     Int
  type      SlideType
  title     String
  content   Json?
  classId   String
  class     Class     @relation(fields: [classId], references: [id], onDelete: Cascade)
  createdAt DateTime  @default(now())

  @@map("slides")
}

enum SlideType {
  COVER
  CONTENT
  ACTIVITY
  VIDEO
  IMAGE
}

// ─── SISTEMA DE CALIFICACIÓN ───────────────────────────────
model GradebookStructure {
  id       String   @id @default(cuid())
  courseId String   @unique
  course   Course   @relation(fields: [courseId], references: [id])
  aspects  Aspect[]

  @@map("gradebook_structures")
}

model Aspect {
  id          String             @id @default(cuid())
  name        String
  weight      Float
  structureId String
  structure   GradebookStructure @relation(fields: [structureId], references: [id])
  indicators  Indicator[]

  @@map("aspects")
}

model Indicator {
  id         String     @id @default(cuid())
  name       String
  weight     Float
  aspectId   String
  aspect     Aspect     @relation(fields: [aspectId], references: [id])
  activities Activity[]

  @@map("indicators")
}

model Activity {
  id           String       @id @default(cuid())
  name         String
  weight       Float
  maxScore     Float        @default(5.0)
  indicatorId  String
  indicator    Indicator    @relation(fields: [indicatorId], references: [id])
  gradeEntries GradeEntry[]

  @@map("activities")
}

// ─── NOTAS ─────────────────────────────────────────────────
model GradeEntry {
  id         String   @id @default(cuid())
  score      Float
  feedback   String?
  userId     String
  activityId String
  periodId   String
  user       User     @relation(fields: [userId], references: [id])
  activity   Activity @relation(fields: [activityId], references: [id])
  period     Period   @relation(fields: [periodId], references: [id])
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([userId, activityId, periodId])
  @@map("grade_entries")
}

// ─── AUTOEVALUACIÓN ────────────────────────────────────────
model SelfEvaluation {
  id        String   @id @default(cuid())
  score     Float
  feedback  String?
  userId    String
  courseId  String
  periodId  String
  user      User     @relation(fields: [userId], references: [id])
  createdAt DateTime @default(now())

  @@unique([userId, courseId, periodId])
  @@map("self_evaluations")
}

// ─── COEVALUACIÓN ──────────────────────────────────────────
model PeerEvaluation {
  id          String   @id @default(cuid())
  score       Float
  feedback    String?
  evaluatorId String
  evaluatedId String
  courseId    String
  periodId    String
  evaluator   User     @relation("EvaluatorUser", fields: [evaluatorId], references: [id])
  evaluated   User     @relation("EvaluatedUser", fields: [evaluatedId], references: [id])
  createdAt   DateTime @default(now())

  @@unique([evaluatorId, evaluatedId, courseId, periodId])
  @@map("peer_evaluations")
}

// ─── GAMIFICACIÓN ─────────────────────────────────────────
model StudentPoints {
  id        String   @id @default(cuid())
  points    Int      @default(0)
  userId    String
  courseId  String
  user      User     @relation(fields: [userId], references: [id])
  course    Course   @relation(fields: [courseId], references: [id])
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([userId, courseId])
  @@map("student_points")
}

model Badge {
  id          String         @id @default(cuid())
  name        String
  description String?
  icon        String?
  isActive    Boolean        @default(true)
  courseId    String
  course      Course         @relation(fields: [courseId], references: [id])
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  assignments StudentBadge[]

  @@map("badges")
}

model StudentBadge {
  id        String   @id @default(cuid())
  userId    String
  badgeId   String
  user      User     @relation(fields: [userId], references: [id])
  badge     Badge    @relation(fields: [badgeId], references: [id], onDelete: Cascade)
  awardedAt DateTime @default(now())

  @@unique([userId, badgeId])
  @@map("student_badges")
}
```

---

## 📦 VARIABLES DE ENTORNO (.env)

```env
PORT=3000
NODE_ENV=development
DATABASE_URL="postgresql://lumina:lumina1234@localhost:5432/lumina"
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET=lumina_super_secret_key_2025_cambiar_en_produccion
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3001
OPENAI_API_KEY=sk-...  # requerida para AiFeaturesModule (Fase 4)
```

---

## 🏗️ ARQUITECTURA DE MÓDULOS

### Módulos completados ✅

#### 1. PrismaModule (Global)
- **Ruta:** `src/prisma/`
- **Descripción:** Cliente de Prisma compartido globalmente
- **Nota importante:** Usa `require()` con ruta absoluta via `process.cwd()` para resolver problemas de rutas en dist/

```typescript
// src/prisma/prisma.service.ts — patrón clave
const { PrismaClient } = require(
  path.join(process.cwd(), 'prisma/generated/prisma')
);
```

#### 2. AuthModule ✅
- **Ruta:** `src/auth/`
- **Endpoints:**
  - `POST /auth/register` — registro de usuarios
  - `POST /auth/login` — login con JWT (rate limited: 10 req/min)
  - `GET /auth/me` — perfil del usuario autenticado
- **Archivos:**
  - `auth.controller.ts`, `auth.service.ts`, `jwt.strategy.ts`
  - `jwt-auth.guard.ts`, `roles.guard.ts`, `roles.decorator.ts`
  - `dto/register.dto.ts`, `dto/login.dto.ts`
- **Características:**
  - ConfigModule + JWT con `ConfigService.getOrThrow`
  - Rate limiting con `@nestjs/throttler` (10 req/min en login)
  - DTOs estrictos con normalización de email y trim
  - Manejo de error P2002 de Prisma
  - Tests unitarios reales

#### 3. UsersModule ✅
- **Ruta:** `src/users/`
- **Endpoints:**
  - `GET /users` — listar usuarios (ADMIN/SUPERADMIN), con paginación
  - `GET /users/:id` — obtener usuario (ADMIN/SUPERADMIN/TEACHER)
  - `PATCH /users/:id` — actualizar usuario (solo ADMIN puede cambiar roles)
  - `DELETE /users/:id` — desactivar usuario (soft delete: `isActive: false`)
- **Archivos:** `users.controller.ts`, `users.service.ts`, `dto/update-user.dto.ts`

#### 4. CoursesModule ✅
- **Ruta:** `src/courses/`
- **Endpoints:**
  - `POST /courses` — crear curso (TEACHER/ADMIN)
  - `GET /courses` — listar cursos (filtrado por rol)
  - `GET /courses/:id` — obtener curso con detalles
  - `PATCH /courses/:id` — actualizar curso
  - `DELETE /courses/:id` — desactivar curso (soft delete)
  - `POST /courses/:id/enroll` — matricular estudiante
  - `GET /courses/:id/students` — listar estudiantes
- **Archivos:** `courses.controller.ts`, `courses.service.ts`, `dto/create-course.dto.ts`, `dto/update-course.dto.ts`, `dto/enroll-student.dto.ts`
- **Lógica de visibilidad:**
  - ADMIN → todos los cursos
  - TEACHER → solo sus cursos (`teacherId = req.user.id`)
  - STUDENT → solo cursos donde tiene `Enrollment`

### Módulos pendientes 🔲

#### 5. ClassesModule ✅
- **Ruta:** `src/classes/`
- **Endpoints:**
  - `POST /classes` — crear clase (body: `{ title, description?, courseId }`)
  - `GET /classes?courseId=` — listar clases del curso (excluye ARCHIVED)
  - `GET /classes/:id` — obtener clase con slides ordenados por `order`
  - `PATCH /classes/:id` — actualizar título/descripción
  - `DELETE /classes/:id` — soft delete (`status: ARCHIVED`)
  - `POST /classes/:id/publish` — cambiar status a PUBLISHED
  - `POST /classes/:id/slides` — agregar slide (auto-incrementa `order`)
  - `PATCH /classes/:id/slides/:slideId` — actualizar slide
  - `DELETE /classes/:id/slides/:slideId` — eliminar slide + reordenar restantes
- **Archivos:**
  - `classes.controller.ts`, `classes.service.ts`, `classes.module.ts`
  - `dto/create-class.dto.ts`, `dto/update-class.dto.ts`
  - `dto/create-slide.dto.ts`, `dto/update-slide.dto.ts`
- **Características:**
  - Código único de clase generado con `nanoid(8)`
  - ADMIN ve todas las clases, TEACHER solo las suyas, STUDENT solo si está matriculado
  - `content` de Slide es `Json` libre — se castea con `as any` para Prisma 7
  - Relación en `create` usa `class: { connect: { id } }` en lugar de `classId` directo

#### 6. ActivitiesModule ✅
- **Ruta:** `src/activities/`
- **Prefijo HTTP:** `courses/:courseId` (subrutas `indicators/.../activities` y `activities/...`)
- **Endpoints:**
  - `POST .../indicators/:indicatorId/activities` — crear actividad (`name`, `weight` 0–1, `maxScore` opcional, default 5.0); suma de pesos por indicador ≤ 1.0
  - `GET .../indicators/:indicatorId/activities` — listar actividades del indicador + `meta.weightSum` y `meta.weightsValid`
  - `GET .../activities/:activityId` — detalle (incluye `_count.gradeEntries`); lectura para quien tenga acceso al curso
  - `PATCH .../activities/:activityId` — actualizar nombre, peso o `maxScore` (misma regla de suma de pesos)
  - `DELETE .../activities/:activityId` — eliminar solo si no hay `GradeEntry`; si hay notas, `409`
- **Permisos:** mutaciones = docente dueño del curso, ADMIN, SUPERADMIN; lectura = igual que otros módulos del curso (matriculados incluidos)
- **DTOs:** `create-activity.dto.ts`, `update-activity.dto.ts` (`maxScore` entre 0.1 y 100 para flexibilidad; escala pedagógica 0–5 documentada en dominio)

#### 7. PerformanceIndicatorsModule ✅
- **Ruta:** `src/performance-indicators/`
- **Prefijo HTTP:** `courses/:courseId/gradebook`
- **Endpoints:**
  - `POST .../gradebook` — crear `GradebookStructure` vacía para el curso (TEACHER dueño / ADMIN / SUPERADMIN); `409` si ya existe
  - `GET .../gradebook` — **estructura** (árbol `aspects` → `indicators` + `meta`); no confundir con el libro de notas: ese es `GET .../grades?periodId=` (`GradebookModule`)
  - `POST .../gradebook/aspects` — crear aspecto (`name`, `weight` 0–1); rechaza si la suma de pesos de aspectos supera 1.0
  - `PATCH .../gradebook/aspects/:aspectId` — actualizar nombre/peso con la misma regla de suma
  - `DELETE .../gradebook/aspects/:aspectId` — elimina indicadores sin actividades; `409` si hay `Activity` bajo algún indicador
  - `POST .../gradebook/aspects/:aspectId/indicators` — crear indicador; suma de pesos por aspecto ≤ 1.0
  - `PATCH .../gradebook/indicators/:indicatorId` — actualizar indicador (misma regla de suma)
  - `DELETE .../gradebook/indicators/:indicatorId` — `409` si el indicador tiene actividades
- **Validación:** suma de pesos no puede exceder 1.0 por nivel (tolerancia `1e-5`); en `GET`, `meta` indica si las sumas cerraron en 1.0 (estructura “completa” por nivel)

#### 8. GradebookModule ✅ ← PRÓXIMO: GradeCalculationModule (§9)
- **Ruta:** `src/gradebook/`
- **Prefijo HTTP:** `courses/:courseId` (comparte prefijo con `ActivitiesModule`; rutas sin colisión)
- **Períodos (`Period`):**
  - `POST .../periods` — crear (`name`, `startDate`, `endDate` ISO, `isActive` opcional)
  - `GET .../periods` — listar del curso (incluye `_count.gradeEntries`)
  - `PATCH .../periods/:periodId` — actualizar
  - `DELETE .../periods/:periodId` — desactivar (`isActive: false`), conserva histórico de notas
- **Calificaciones (`GradeEntry`):**
  - `POST .../grade-entries` — upsert por (`userId`, `activityId`, `periodId`); exige estudiante matriculado, período y actividad del curso; `score` ≤ `Activity.maxScore`
  - `PATCH .../grade-entries/:entryId` — actualizar `score` y/o `feedback`
- **Libro por período:**
  - `GET .../grades?periodId=` — matriz: `activities` (planas con `aspectName` / `indicatorName`), `rows[]` con `user` y `cells[]` (`entry` o `null`); docente/admin ven todos los matriculados; **STUDENT** solo su fila
- **Requisito:** debe existir `GradebookStructure` del curso; si no, `404` en `GET .../grades`

#### 9. GradeCalculationModule ✅ — completado 25/03/2026
- **Ruta:** `src/grade-calculation/`
- **Prefijo HTTP:** `courses/:courseId`
- **Endpoints:**
  - `GET .../grade-calculation?periodId=` — resumen compacto de todos los estudiantes (nota final + isComplete)
  - `GET .../grade-calculation/full?periodId=` — desglose multinivel completo de todos los estudiantes
  - `GET .../grade-calculation/students/:userId?periodId=` — desglose individual; STUDENT solo ve la suya
- **Permisos:** TEACHER/ADMIN/SUPERADMIN ven todos; STUDENT solo su propia nota
- **Lógica clave:**
  - Una sola query a `GradeEntry` para todos los activities del curso (sin N+1)
  - Cálculo parcial si faltan entradas: devuelve lo disponible con `isComplete: false`
  - Valida que suma de `Aspect.weight` sea exactamente 0.90 antes de calcular
  - Redondeo a 2 decimales en todos los valores
- **Archivos:** `grade-calculation.module.ts`, `grade-calculation.service.ts`, `grade-calculation.controller.ts`, `dto/calculate-grade.dto.ts`

#### 10. SelfEvaluationModule ✅ — completado 25/03/2026
- **Ruta:** `src/self-evaluation/`
- **Prefijo HTTP:** `courses/:courseId/self-evaluations`
- **Regla de dominio:** el docente pregunta la nota al estudiante y él mismo la ingresa — el estudiante nunca escribe
- **Endpoints:**
  - `POST .../self-evaluations` — docente registra la autoevaluación (`userId`, `periodId`, `score` 0–5, `feedback?`)
  - `GET .../self-evaluations?periodId=` — lista todas del curso en el período (TEACHER/ADMIN/SUPERADMIN)
  - `GET .../self-evaluations/students/:userId?periodId=` — ver una; STUDENT solo la suya
  - `PATCH .../self-evaluations/students/:userId?periodId=` — actualizar (solo docente)
  - `DELETE .../self-evaluations/students/:userId?periodId=` — eliminar (solo docente)
- **Restricciones:** unique por `(userId, courseId, periodId)`; estudiante debe estar matriculado; período debe pertenecer al curso
- **Archivos:** `self-evaluation.module.ts`, `self-evaluation.service.ts`, `self-evaluation.controller.ts`, `dto/create-self-evaluation.dto.ts`, `dto/update-self-evaluation.dto.ts`

#### 11. PeerEvaluationModule ✅ — completado 25/03/2026
- **Ruta:** `src/peer-evaluation/`
- **Prefijo HTTP:** `courses/:courseId/peer-evaluations`
- **Regla de dominio:** el docente pregunta al grupo y él mismo ingresa las notas — el estudiante nunca escribe
- **Endpoints:**
  - `POST .../peer-evaluations` — docente registra (`evaluatorId`, `evaluatedId`, `periodId`, `score` 0–5, `feedback?`)
  - `GET .../peer-evaluations?periodId=` — lista todas del curso (TEACHER/ADMIN/SUPERADMIN)
  - `GET .../peer-evaluations/students/:evaluatedId?periodId=` — coevaluaciones recibidas + promedio; STUDENT solo las suyas
  - `PATCH .../peer-evaluations/:id` — actualizar (solo docente)
  - `DELETE .../peer-evaluations/:id` — eliminar (solo docente)
- **Restricciones:** `evaluatorId !== evaluatedId`; ambos deben estar matriculados; unique por `(evaluatorId, evaluatedId, courseId, periodId)`
- **Archivos:** `peer-evaluation.module.ts`, `peer-evaluation.service.ts`, `peer-evaluation.controller.ts`, `dto/create-peer-evaluation.dto.ts`, `dto/update-peer-evaluation.dto.ts`

#### 12. LiveSessionsModule 🔲
- Socket.IO: unirse a una clase en vivo, sincronizar slides, recibir actividades en tiempo real

#### 13. H5pActivitiesModule ✅ — completado 25/03/2026 (Fase 2)
- **Ruta:** `src/h5p-activities/`
- **Prefijo HTTP:** `courses/:courseId/classes/:classId/h5p-activities`
- **Descripción:** Gestiona actividades interactivas H5P embebidas en slides de tipo `ACTIVITY`. El contenido H5P se almacena como JSON en el campo `content` del modelo `Slide` con la estructura `{ h5pType, params }`.
- **Tipos H5P soportados:** `Quiz`, `Flashcards`, `DragAndDrop`, `FillInTheBlanks`, `MultipleChoice`
- **Endpoints:**
  - `POST .../h5p-activities` — crear actividad H5P (genera un `Slide` de tipo `ACTIVITY` con content JSON); auto-incrementa `order`
  - `GET .../h5p-activities` — listar todas las actividades H5P de la clase (slides `type = ACTIVITY`) ordenadas por `order`
  - `GET .../h5p-activities/:slideId` — obtener actividad H5P por `slideId`
  - `PATCH .../h5p-activities/:slideId` — actualizar `title`, `h5pType` y/o `params`; merge parcial del content
  - `DELETE .../h5p-activities/:slideId` — eliminar slide H5P y reordenar los restantes
- **Permisos:** mutaciones = docente titular del curso, ADMIN, SUPERADMIN; lectura = todos con acceso al curso (matriculados incluidos)
- **Validación:** verifica que el curso exista, que la clase pertenezca al curso, y que el slide sea de tipo `ACTIVITY`; reordena slides al eliminar
- **Almacenamiento:** no crea nuevo modelo en DB — reutiliza el modelo `Slide` con `type: 'ACTIVITY'` y `content: { h5pType: H5pType, params: Record<string, unknown> }`
- **Scope de autorización:** `'h5pActivities'` (agregado a `CourseManageScope` en `course-authorization.service.ts`)
- **Archivos:** `h5p-activities.module.ts`, `h5p-activities.service.ts`, `h5p-activities.controller.ts`, `dto/create-h5p-activity.dto.ts`, `dto/update-h5p-activity.dto.ts`

#### 14. GamificationModule ✅ — completado 25/03/2026 (Fase 2)
- **Ruta:** `src/gamification/`
- **Prefijo HTTP:** `courses/:courseId/gamification`
- **Descripción:** Sistema de puntos e insignias (badges) por curso. Los docentes asignan puntos y badges a estudiantes matriculados; los estudiantes ven sus propios datos o el leaderboard global.
- **Modelos Prisma nuevos (migración `20260326_gamification`):**
  - `StudentPoints` — puntos acumulados por estudiante en un curso; campos: `points`, `reason?`; unique `(userId, courseId)`; upsert pattern
  - `Badge` — definición de insignia para un curso; soft delete (`isActive`)
  - `StudentBadge` — asignación badge ↔ estudiante; unique `(userId, badgeId)`; eliminación física al revocar
- **Endpoints — Puntos:**
  - `POST .../gamification/points` — upsert de puntos (`userId`, `points` entero ≥ 0, `reason?` string opcional); reemplaza el total actual
  - `GET .../gamification/leaderboard` — ranking de todos los estudiantes con puntos, ordenado desc; visible para todos los matriculados
  - `GET .../gamification/students/:userId/points` — puntos de un estudiante; STUDENT solo ve los suyos
- **Endpoints — Badges:**
  - `POST .../gamification/badges` — crear insignia (`name`, `description?`, `icon?`)
  - `GET .../gamification/badges` — listar insignias activas + `_count.assignments`
  - `GET .../gamification/badges/:badgeId` — detalle de insignia
  - `PATCH .../gamification/badges/:badgeId` — actualizar nombre / descripción / icono
  - `DELETE .../gamification/badges/:badgeId` — soft delete (`isActive: false`)
  - `POST .../gamification/badges/:badgeId/assign` — asignar badge a estudiante (`userId`); `409` si ya tiene
  - `DELETE .../gamification/badges/:badgeId/students/:userId` — revocar badge (elimina StudentBadge)
  - `GET .../gamification/students/:userId/badges` — badges ganados por estudiante; STUDENT solo ve los suyos
- **Permisos:** mutaciones = docente titular, ADMIN, SUPERADMIN; lectura leaderboard/badges = todos los matriculados; puntos y badges propios = STUDENT; datos de otros = solo TEACHER/ADMIN/SUPERADMIN
- **Scope de autorización:** `'gamification'` (agregado a `CourseManageScope`)
- **Relaciones nuevas en User:** `studentPoints StudentPoints[]`, `studentBadges StudentBadge[]`
- **Relaciones nuevas en Course:** `studentPoints StudentPoints[]`, `badges Badge[]`
- **Archivos:** `gamification.module.ts`, `gamification.service.ts`, `gamification.controller.ts`, `dto/assign-points.dto.ts`, `dto/create-badge.dto.ts`, `dto/update-badge.dto.ts`, `dto/assign-badge.dto.ts`

#### 15. ClassEditorModule ✅ — completado 25/03/2026 (Fase 2)
- **Ruta:** `src/class-editor/`
- **Prefijo HTTP:** `classes/:classId/editor`
- **Descripción:** Operaciones avanzadas de edición sobre clases y slides. No crea modelos nuevos — opera directamente sobre `Class` y `Slide` existentes.
- **Endpoints:**
  - `PATCH .../editor/slides/reorder` — reordenar slides; body `{ slides: [{id, order},...] }`; valida que todos los IDs pertenecen a la clase; transacción atómica
  - `PATCH .../editor/slides/bulk` — bulk update de `title` y/o `content` de múltiples slides en una transacción; valida ownership de todos los IDs
  - `PATCH .../editor/slides/:slideId/type` — cambiar `type` (SlideType enum) de una slide
  - `POST  .../editor/slides/:slideId/duplicate` — duplicar slide al final de la clase; título con sufijo `" (copia)"`
  - `POST  .../editor/duplicate` — duplicar clase completa (nuevo `code` con `nanoid(8)`, status `DRAFT`, todas las slides copiadas en orden)
  - `GET   .../editor/preview` — clase completa con slides ordenadas; solo lectura
- **Orden de rutas en controller:** `slides/reorder` y `slides/bulk` (literales) registrados antes de `slides/:slideId/*` (params) para evitar conflicto de matching
- **Permisos:** mutaciones = docente titular del curso, ADMIN, SUPERADMIN; preview (`GET`) = todos los matriculados (`verifyCourseReadAccess`)
- **Scope de autorización:** `'classEditor'` (agregado a `CourseManageScope`)
- **Transacciones:** `reorderSlides` y `bulkUpdateSlides` usan `prisma.$transaction([...])` para atomicidad
- **Archivos:** `class-editor.module.ts`, `class-editor.service.ts`, `class-editor.controller.ts`, `dto/reorder-slides.dto.ts`, `dto/change-slide-type.dto.ts`, `dto/bulk-update-slides.dto.ts`

#### 16. MessagingModule ✅ — completado 25/03/2026 (Fase 2)
- **Ruta:** `src/messaging/`
- **Prefijo HTTP:** `courses/:courseId/messages`
- **Descripción:** Sistema de mensajería interna por curso entre docente y estudiantes. Soporta mensajes de canal (todos), hilos de respuestas y mensajes directos (DM) entre dos miembros del curso.
- **Modelos Prisma nuevos (migración `20260326012942_messaging`):**
  - `Message` — cuerpo (`content`), `isDeleted`, `courseId`, `senderId`, `recipientId?` (null = canal, set = DM), `parentId?` (null = top-level, set = reply); auto-relación `parent/replies`
  - `MessageRead` — registro de lectura por usuario; unique `(messageId, userId)`
- **Endpoints:**
  - `POST .../messages` — enviar mensaje en el canal del curso (`content`, `parentId?`)
  - `GET .../messages` — listar top-level mensajes de canal, paginado desc; sin DMs
  - `GET .../messages/:messageId` — obtener mensaje con replies (no eliminados) y reads
  - `POST .../messages/:messageId/read` — marcar como leído (no puede ser el autor)
  - `DELETE .../messages/:messageId` — soft delete propio (`isDeleted: true`, `content: ''`); solo el autor
  - `POST .../messages/direct` — enviar DM (`content`, `recipientId`, `parentId?`); valida que el destinatario sea miembro
  - `GET .../messages/direct/:userId` — historial de DMs entre el usuario autenticado y `:userId`, paginado desc
- **Orden de rutas en controller:** `direct` y `direct/:userId` (literales) registrados antes de `:messageId` (param) para evitar conflicto
- **Permisos:** cualquier miembro del curso (matriculado o docente) puede enviar y leer; solo el autor puede eliminar su mensaje; el lector de un `read` no puede ser el autor
- **Validación DM:** el destinatario debe ser estudiante matriculado o el docente titular del curso
- **Relaciones nuevas en User:** `sentMessages Message[]`, `receivedMessages Message[]`, `messageReads MessageRead[]`
- **Relaciones nuevas en Course:** `messages Message[]`
- **Archivos:** `messaging.module.ts`, `messaging.service.ts`, `messaging.controller.ts`, `dto/send-message.dto.ts`, `dto/send-direct-message.dto.ts`

#### 17. AnalyticsModule ✅ — completado 25/03/2026 (Fase 3)
- **Ruta:** `src/analytics/`
- **Prefijo HTTP:** `courses/:courseId/analytics`
- **Descripción:** Métricas y estadísticas agregadas por curso y por estudiante. Todo se calcula en tiempo real desde los modelos existentes — sin modelos nuevos en DB.
- **Query param común:** `periodId?` — filtra las métricas de notas por período académico
- **Endpoints:**
  - `GET .../analytics/summary?periodId=` — resumen del curso: `totalStudents`, `avgGrade`, `completionRate`, desglose de clases por estado
  - `GET .../analytics/students?periodId=` — progreso de todos los estudiantes (staff); STUDENT recibe solo su fila
  - `GET .../analytics/students/:userId?periodId=` — progreso individual: actividades completadas, nota promedio, tendencia (`up`/`down`/`stable`/`null`); STUDENT solo ve la suya
  - `GET .../analytics/activities?periodId=` — ranking de actividades por avg score normalizado (desc); incluye `best` y `worst`
  - `GET .../analytics/live-sessions` — conteo de clases por estado (`draft`, `published`, `live`, `archived`) + lista detallada; sin tracking por estudiante (requiere modelo de asistencia)
  - `GET .../analytics/engagement` — por estudiante: `messagesSent`, `points`, `badgesEarned`; STUDENT solo ve el suyo
  - `GET .../analytics/export?periodId=` — JSON completo: `course` + `summary` + `studentsProgress` + `activitiesRanking` + `engagement`; solo staff
- **Permisos:** TEACHER/ADMIN/SUPERADMIN ven todos los datos; STUDENT solo ve sus propias métricas (en `students`, `students/:userId`, `engagement`); `export` exclusivo para staff
- **Cálculo de tendencia:** compara promedio de primera mitad vs segunda mitad de GradeEntries (cronológicas); requiere ≥ 4 entradas; umbral ±0.1
- **Completitud:** `completionRate = gradeEntriesCount / (totalStudents × totalActivities)` — solo aproximado si hay múltiples períodos sin `periodId`
- **Live sessions:** no hay modelo de asistencia por estudiante; se exponen datos de nivel de clase como proxy; se incluye `note` en la respuesta indicando la limitación
- **Sin modelos nuevos en DB** — opera sobre: `Enrollment`, `GradeEntry`, `Activity`, `Indicator`, `Aspect`, `GradebookStructure`, `Class`, `Message`, `StudentPoints`, `StudentBadge`, `Badge`
- **Archivos:** `analytics.module.ts`, `analytics.service.ts`, `analytics.controller.ts`

#### 18. GradebookReportsModule ✅ — completado 25/03/2026 (Fase 3)
- **Ruta:** `src/gradebook-reports/`
- **Prefijo HTTP:** `courses/:courseId/reports`
- **Descripción:** Reportes detallados del libro de calificaciones por curso y período. Todos los endpoints son exclusivos para staff (docente/ADMIN/SUPERADMIN). Sin modelos nuevos — usa los existentes.
- **Dependencia:** importa `GradeCalculationModule` para reutilizar `GradeCalculationService.calculateForStudent` y `getSummary` en el desglose individual y notas finales.
- **Query param:** `periodId` — **obligatorio** en todos los endpoints; lanza 400 si no se proporciona.
- **Scope de autorización:** `'reports'` (nuevo, agregado a `CourseManageScope` en `course-authorization.service.ts`)
- **Endpoints:**
  - `GET .../reports/matrix?periodId=` — matriz estudiantes × actividades: por cada fila (estudiante) → `cells[]` (score, normalizedScore, feedback por actividad), `selfEvaluation`, `peerEvaluation`, `processScore`, `finalGrade`, `isComplete`; eficiente: ~5 queries sin N+1
  - `GET .../reports/students/:userId?periodId=` — desglose completo de un estudiante (delega a `GradeCalculationService.calculateForStudent`): árbol aspectos → indicadores → actividades con pesos y notas ponderadas
  - `GET .../reports/final-grades?periodId=` — lista compacta de estudiantes con nota final + `isComplete`; delega a `GradeCalculationService.getSummary`
  - `GET .../reports/distribution?periodId=` — distribución de notas en rangos `0–1`, `1–2`, `2–3`, `3–4`, `4–5` con count y lista de estudiantes; incluye `withoutGrade[]` y `average`
  - `GET .../reports/pending?periodId=` — por estudiante: actividades sin GradeEntry, `completedCount`, `pendingCount`, `completionPct`; ordenado por más pendientes primero
  - `GET .../reports/export?periodId=` — JSON completo en una sola llamada: `finalGrades` + `distribution` + `pending`; eficiente: llama `loadReportData` una sola vez y computa todo en memoria
- **Permisos:** exclusivo staff — STUDENT no tiene acceso a ningún endpoint
- **Orden de rutas en controller:** literales (`matrix`, `final-grades`, `distribution`, `pending`, `export`) antes del param (`students/:userId`)
- **Eficiencia matriz/pending/export:** carga datos una vez con `loadReportData()` (~5 queries en paralelo con `Promise.all`): estructura del gradebook, enrollments, todos los GradeEntries del período, todos los SelfEvaluations del período, todos los PeerEvaluations del período. Computa nota final en memoria sin queries adicionales.
- **Archivos:** `gradebook-reports.module.ts`, `gradebook-reports.service.ts`, `gradebook-reports.controller.ts`

#### 19. PerformanceTrackingModule ✅ — completado 25/03/2026 (Fase 3)
- **Ruta:** `src/performance-tracking/`
- **Prefijos HTTP:** `courses/:courseId/performance` y `students/:userId/performance`
- **Descripción:** Seguimiento del desempeño académico temporal por estudiante y curso. Sin modelos nuevos — calcula todo desde los existentes.
- **Dependencia:** importa `GradeCalculationModule` para reutilizar `GradeCalculationService.calculateForStudent` en el endpoint breakdown.
- **Scope de autorización:** `'reports'` (reutiliza el mismo scope agregado en GradebookReports)
- **Carga de datos eficiente:** `_loadData(courseId)` — ~6 queries en paralelo con `Promise.all`; construye `entryMap`, `selfEvalMap`, `peerEvalMap` como Maps anidados para O(1) lookup; `_computeGrade()` es función pura en memoria.
- **Endpoints (controlador principal):**
  - `GET .../performance/ranking` — staff only; ranking de todos los estudiantes por nota promedio sobre todos los períodos; incluye `gradePerPeriod[]` y `average`
  - `GET .../performance/at-risk` — staff only; filtra por período activo; alerta si `finalGrade < 3.0` o `completionPct < 50%`; incluye `riskReasons[]`
  - `GET .../performance/evolution/:userId` — evolución temporal de notas del estudiante por período; accesible por staff o el propio estudiante
  - `GET .../performance/comparison/:userId` — nota del estudiante vs promedio del curso por período; accesible por staff o el propio estudiante
  - `GET .../performance/breakdown/:userId?periodId=` — delega a `GradeCalculationService.calculateForStudent`; `periodId` obligatorio (400 si falta)
- **Endpoint historial cruzado (segundo controlador):**
  - `GET /students/:userId/performance/history` — historial completo del estudiante en todos sus cursos; solo ADMIN/SUPERADMIN o el propio usuario; usa `Promise.all` por curso
- **Permisos:** staff ve todo; STUDENT solo ve sus propios datos; `getCrossCourseHistory` solo ADMIN/SUPERADMIN o self (no hay contexto de curso para verificar TEACHER)
- **Orden de rutas en controller:** literales (`ranking`, `at-risk`) antes de rutas con param (`evolution/:userId`, `comparison/:userId`, `breakdown/:userId`)
- **Dos controladores en un módulo:** `PerformanceTrackingController` + `StudentHistoryController`
- **Archivos:** `performance-tracking.module.ts`, `performance-tracking.service.ts`, `performance-tracking.controller.ts`, `student-history.controller.ts`

#### 20. AiFeaturesModule ✅ — completado 25/03/2026 (Fase 4)
- **Ruta:** `src/ai-features/`
- **Prefijos HTTP:** `ai` (standalone) y `courses/:courseId/ai` (con contexto de curso)
- **Descripción:** Integración con OpenAI API (gpt-4o-mini) para funcionalidades de IA educativa. Sin modelos nuevos en DB. Solo accesible por personal docente.
- **Dependencias:** `openai@6.x` (pnpm add openai); `ConfigService` para `OPENAI_API_KEY`
- **Variable de entorno:** `OPENAI_API_KEY` — si no está configurada, cada endpoint retorna `ServiceUnavailableException` con mensaje claro (no rompe la app)
- **Manejo de errores:** todas las llamadas a OpenAI envueltas en try/catch → `ServiceUnavailableException`; respuestas JSON inválidas → `ServiceUnavailableException`
- **Permisos:** solo `TEACHER`, `TEACHER_ASSISTANT`, `DEPARTMENT_HEAD`, `ADMIN`, `SUPERADMIN`; verificado en `assertStaff()` helper o via `courseAuth.assertStaffCanManageCourse`
- **Dos controllers en un módulo:** `AiFeaturesController` + `CourseAiController`
- **Endpoints standalone (`/ai`):**
  - `POST /ai/quiz` — genera preguntas de quiz (MultipleChoice/TrueFalse/FillInTheBlanks) a partir de texto/tema; body: `{ text, count?, type? }`; devuelve `{ type, count, questions[] }`
  - `POST /ai/content-assistant` — genera estructura completa de clase (title, description, objectives, slides[]) a partir de un tema; body: `{ topic, slideCount?, level? }`
  - `POST /ai/evaluate-response` — evalúa y puntúa respuesta libre de estudiante; body: `{ question, studentResponse, rubric?, maxScore? }`; devuelve `{ score, percentage, qualitativeGrade, strengths[], weaknesses[], feedback, suggestedAnswer }`
- **Endpoints con contexto de curso (`/courses/:courseId/ai`):**
  - `POST .../ai/student-feedback` — retroalimentación personalizada basada en GradeEntries + SelfEval + PeerEval del período; body: `{ studentId, periodId }`; carga datos reales del DB antes de llamar a OpenAI
  - `POST .../ai/class-summary` — resumen automático de clase a partir de sus slides; body: `{ classId }`; verifica que la clase pertenezca al curso; 400 si no hay slides
- **Modelo OpenAI:** `gpt-4o-mini` con `temperature: 0.7`, `max_tokens: 2000`, `response_format: json_object`
- **Archivos:** `ai-features.module.ts`, `ai-features.service.ts`, `ai-features.controller.ts`, `course-ai.controller.ts`, `dto/generate-quiz.dto.ts`, `dto/student-feedback.dto.ts`, `dto/class-summary.dto.ts`, `dto/content-assistant.dto.ts`, `dto/evaluate-response.dto.ts`

#### 21. CollaborationModule ✅ — completado 25/03/2026 (Fase 4)
- **Ruta:** `src/collaboration/`
- **Prefijos HTTP:** `courses/:courseId/collaboration` (grupos) y `courses/:courseId/classes/:classId/collaboration` (pizarras y notas)
- **Descripción:** Funcionalidades de colaboración en tiempo real entre estudiantes: pizarras Fabric.js, notas Tiptap, grupos de trabajo y asignación de actividades a grupos.
- **Modelos Prisma nuevos (6):** `WhiteboardSession`, `CollabNote`, `NoteVersion`, `WorkGroup`, `WorkGroupMember`, `GroupActivity`
- **Relaciones agregadas a modelos existentes:** `Class` → `whiteboardSessions`, `collabNotes`; `Course` → `workGroups`; `User` → `workGroupMemberships`; `Activity` → `groupActivities`
- **Dos controllers en un módulo:** `CollaborationController` + `ClassCollabController`
- **Permisos:**
  - Staff (TEACHER/ADMIN/SUPERADMIN/etc.): crear/eliminar pizarras, notas y grupos; gestionar miembros y actividades de grupo
  - Cualquier miembro del curso: listar/leer pizarras y notas; **editar contenido** (PATCH) de pizarras y notas colaborativas
  - `addMember` verifica que el usuario esté matriculado en el curso antes de agregar al grupo
- **Soft delete:** pizarras (`isActive: false`), notas (`isActive: false`), grupos (`isActive: false`)
- **Historial de versiones:** `PATCH .../notes/:noteId` guarda automáticamente snapshot del contenido anterior como `NoteVersion` antes de aplicar el cambio; solo cuando hay `content` nuevo y la nota ya tenía contenido
- **Validaciones:**
  - `addMember` — verifica matrícula; P2002 → ConflictException
  - `assignActivity` — verifica que la actividad pertenece al curso (via `indicator.aspect.structure.courseId`)
  - Clase verificada contra curso en cada operación de pizarra/nota
- **Orden de rutas:** `notes/:noteId/history` registrado antes de `notes/:noteId` para evitar conflicto de params
- **Endpoints (ClassCollabController — `courses/:courseId/classes/:classId/collaboration`):**
  - `POST .../whiteboards` — crear sesión de pizarra (Fabric.js JSON)
  - `GET .../whiteboards` — listar sesiones activas (sin content para listar)
  - `GET .../whiteboards/:sessionId` — obtener con content completo
  - `PATCH .../whiteboards/:sessionId` — actualizar name y/o content (cualquier miembro)
  - `DELETE .../whiteboards/:sessionId` — soft delete (staff)
  - `POST .../notes` — crear nota colaborativa (Tiptap JSON)
  - `GET .../notes` — listar notas activas (sin content)
  - `GET .../notes/:noteId/history` — historial de versiones con timestamp y authorId
  - `GET .../notes/:noteId` — obtener con content completo
  - `PATCH .../notes/:noteId` — editar (cualquier miembro); guarda versión automáticamente
  - `DELETE .../notes/:noteId` — soft delete (staff)
- **Endpoints (CollaborationController — `courses/:courseId/collaboration`):**
  - `POST .../groups` — crear grupo (staff)
  - `GET .../groups` — listar grupos activos con count de miembros y actividades
  - `GET .../groups/:groupId` — detalle con miembros y actividades asignadas
  - `DELETE .../groups/:groupId` — soft delete (staff)
  - `POST .../groups/:groupId/members` — añadir miembro (verifica matrícula)
  - `DELETE .../groups/:groupId/members/:memberId` — eliminar miembro
  - `POST .../groups/:groupId/activities` — asignar actividad del curso al grupo
  - `GET .../groups/:groupId/activities` — listar actividades asignadas
  - `DELETE .../groups/:groupId/activities/:groupActivityId` — desasignar actividad
- **Archivos:** `collaboration.module.ts`, `collaboration.service.ts`, `collaboration.controller.ts`, `class-collab.controller.ts`, DTOs: `create-whiteboard`, `update-whiteboard`, `create-note`, `update-note`, `create-group`, `add-member`, `assign-activity`

#### 22. ForumsModule ✅ — completado 25/03/2026 (Fase 4)
- **Ruta:** `src/forums/`
- **Prefijo HTTP:** `courses/:courseId/forums`
- **Descripción:** Sistema de foros de discusión por curso. Foros (categorías) → Hilos → Respuestas. Soft delete en todos los niveles.
- **Modelos Prisma nuevos (3):** `Forum`, `ForumThread`, `ForumReply`
- **Relación agregada:** `Course` → `forums Forum[]`
- **Permisos:**
  - Staff (TEACHER/ADMIN/SUPERADMIN/etc.): crear/editar/eliminar foros; pin/lock de hilos; eliminar cualquier hilo o respuesta
  - Cualquier miembro del curso: crear hilos y respuestas; editar **solo sus propias** respuestas; eliminar sus propios hilos/respuestas
  - Hilo cerrado (`isLocked: true`): solo staff puede responder
- **Funcionalidades:**
  - `POST/GET/PATCH/DELETE .../forums` — CRUD de categorías de foro (staff)
  - `POST .../forums/:forumId/threads` — crear hilo (cualquier miembro)
  - `GET .../forums/:forumId` — detalle del foro con lista de hilos (título, isPinned, isLocked, count de respuestas)
  - `GET .../forums/:forumId/threads/:threadId` — hilo completo con todas sus respuestas activas
  - `POST .../forums/:forumId/threads/:threadId/pin` — toggle pin (staff)
  - `POST .../forums/:forumId/threads/:threadId/lock` — toggle lock (staff)
  - `DELETE .../forums/:forumId/threads/:threadId` — eliminar hilo (autor o staff)
  - `POST/PATCH/DELETE .../forums/:forumId/threads/:threadId/replies` — gestión de respuestas
- **Archivos:** `forums.module.ts`, `forums.service.ts`, `forums.controller.ts`, DTOs: `create-forum`, `update-forum`, `create-thread`, `create-reply`, `update-reply`

#### 23. IntegrationsModule ✅ — completado 25/03/2026 (Fase 4)
- **Ruta:** `src/integrations/`
- **Prefijo HTTP:** `integrations`
- **Descripción:** Sistema de webhooks salientes. El staff configura URLs externas que reciben notificaciones HTTP POST firmadas (HMAC-SHA256) cuando ocurren eventos en la plataforma.
- **Modelos Prisma nuevos (2):** `Webhook` (url, events[], secret?, courseId?, isActive), `WebhookLog` (statusCode, response, success, sentAt)
- **Exporta `IntegrationsService`** — otros módulos pueden llamar `dispatch(event, data, courseId?)` para disparar webhooks sin acoplarse al módulo
- **10 eventos soportados:** `student.enrolled`, `student.unenrolled`, `grade.submitted`, `grade.updated`, `session.started`, `session.ended`, `message.sent`, `forum.thread.created`, `forum.reply.created`, `badge.awarded`
- **Firma HMAC-SHA256:** si el webhook tiene `secret`, cada POST incluye header `X-Lumina-Signature: sha256=<hex>`
- **Webhooks globales vs por curso:** `courseId: null` = global (solo ADMIN/SUPERADMIN); `courseId: string` = scoped (staff del curso)
- **Timeout de dispatch:** 10 segundos por webhook; fallos no rompen el flujo — se registran en `WebhookLog`
- **Dispatch paralelo:** `Promise.allSettled` sobre todos los webhooks activos suscritos al evento
- **Endpoints:**
  - `GET /integrations/events` — lista eventos válidos + descripción (público para staff)
  - `POST /integrations/webhooks` — crear webhook (body: name, url, events[], secret?, courseId?)
  - `GET /integrations/webhooks?courseId=` — listar webhooks (global o filtrado por curso)
  - `GET /integrations/webhooks/:webhookId` — detalle
  - `PATCH /integrations/webhooks/:webhookId` — actualizar (name, url, secret, events, isActive)
  - `DELETE /integrations/webhooks/:webhookId` — soft delete
  - `POST /integrations/webhooks/:webhookId/test` — disparo manual con evento `webhook.test`
  - `GET /integrations/webhooks/:webhookId/logs?page=&limit=` — historial paginado de envíos
- **Archivos:** `integrations.module.ts`, `integrations.service.ts`, `integrations.controller.ts`, DTOs: `create-webhook`, `update-webhook`, `trigger-webhook`

#### 24. NotificationsModule 🔲

---

## 🔩 MÓDULO RAÍZ — app.module.ts

```typescript
// Imports ya registrados en AppModule (NO duplicar):
// - ConfigModule.forRoot({ isGlobal: true })
// - ThrottlerModule.forRoot([{ ttl: 60_000, limit: 10 }])  ← ttl en ms
// - PrismaModule (global)
// - AuthModule
// - UsersModule
// - CoursesModule
// - ClassesModule
// - PerformanceIndicatorsModule
// - ActivitiesModule
// - GradebookModule
// - GradeCalculationModule   ← Fase 1 completa 25/03/2026
// - SelfEvaluationModule     ← Fase 1 completa 25/03/2026
// - PeerEvaluationModule     ← Fase 1 completa 25/03/2026
// - LiveSessionsModule
// - H5pActivitiesModule      ← Fase 2 completa 25/03/2026
// - GamificationModule       ← Fase 2 completa 25/03/2026
// - ClassEditorModule        ← Fase 2 completa 25/03/2026
// - MessagingModule          ← Fase 2 completa 25/03/2026
// - AnalyticsModule               ← Fase 3 completa 25/03/2026
// - GradebookReportsModule        ← Fase 3 completa 25/03/2026
// - PerformanceTrackingModule     ← Fase 3 completa 25/03/2026 (dos controllers: PerformanceTrackingController + StudentHistoryController)
// - AiFeaturesModule              ← Fase 4 completa 25/03/2026 (dos controllers: AiFeaturesController + CourseAiController)
// - CollaborationModule           ← Fase 4 completa 25/03/2026 (dos controllers: CollaborationController + ClassCollabController)
// - ForumsModule                  ← Fase 4 completa 25/03/2026
// - IntegrationsModule            ← Fase 4 completa 25/03/2026 (exporta IntegrationsService.dispatch)
```

Al agregar nuevos módulos, solo añadir el import en `app.module.ts` — no re-registrar ConfigModule ni ThrottlerModule.

---

## 👥 ROLES DEL SISTEMA

```typescript
enum Role {
  SUPERADMIN
  ADMIN
  DEPARTMENT_HEAD
  TEACHER
  TEACHER_ASSISTANT
  STUDENT
  PARENT
  GUEST
}
```

---

## 📊 SISTEMA DE CALIFICACIÓN

### Regla de dominio fundamental
**El docente es siempre quien ingresa las notas en la plataforma**, sin excepción. Esto aplica a:
- Notas de actividades (`GradeEntry`) — el docente las registra tras evaluar al estudiante
- Autoevaluación (`SelfEvaluation`) — el docente pregunta al estudiante y él mismo la ingresa
- Coevaluación (`PeerEvaluation`) — el docente pregunta al grupo y él mismo las ingresa

El estudiante **nunca escribe notas**. Solo tiene acceso de lectura (`GET`) sobre sus propios registros.

### Fórmula final

```
Nota Final = (Aspectos × 90%) + (Autoevaluación × 5%) + (Coevaluación × 5%)
```

Donde el 90% de Aspectos se distribuye internamente según la configuración del docente. Ejemplo típico:
```
Proceso     = weight 0.70  → 70% de la nota
Convivencia = weight 0.20  → 20% de la nota
─────────────────────────────────────────────
Subtotal Aspectos          = 0.90 (90%)
Autoevaluación             = 0.05 (5%)
Coevaluación               = 0.05 (5%)
─────────────────────────────────────────────
Total                      = 1.00 → máximo 5.0
```

### Restricción crítica de pesos
- Suma de `Aspect.weight` en una `GradebookStructure` **debe ser exactamente 0.90** (no 1.0)
- Los 0.10 restantes están reservados para SelfEvaluation (0.05) y PeerEvaluation (0.05)
- Si la suma de aspectos supera 0.90 la nota final puede superar 5.0 — **no permitido**
- `PerformanceIndicatorsModule` valida este límite al crear/editar aspectos (tolerancia `1e-5`)
- `GradeCalculationModule` valida que la suma sea exactamente 0.90 antes de calcular y lanza error descriptivo si no

### Estructura jerárquica en DB

```
GradebookStructure (1 por curso)
  └── Aspect[]           ← Ej: "Proceso" (weight=0.70), "Convivencia" (weight=0.20)
        └── Indicator[]  ← Ej: "Responsabilidad" (weight=0.33)
              └── Activity[] ← Ej: "Taller 1" (weight=0.20, maxScore=5.0)
                    └── GradeEntry[] ← score del estudiante (por período) — ingresado por el docente
```

### Cálculo multinivel

```
nota_actividad    = GradeEntry.score / Activity.maxScore × 5.0
nota_indicador    = Σ (nota_actividad × Activity.weight)
nota_aspecto      = Σ (nota_indicador × Indicator.weight)
processScore      = Σ (nota_aspecto × Aspect.weight)   ← ya incluye todos los aspectos ponderados
nota_autoeval     = SelfEvaluation.score               ← ingresada por el docente
nota_coevaluacion = promedio(PeerEvaluation.score donde evaluatedId = estudiante) ← ingresada por el docente
nota_final        = processScore + (nota_autoeval × 0.05) + (nota_coevaluacion × 0.05)
```

### Comportamiento con datos incompletos
- Si faltan `GradeEntry` en algunas actividades, el cálculo es parcial — devuelve lo que hay
- `isComplete: false` en la respuesta indica que faltan entradas
- `SelfEvaluation` y `PeerEvaluation` son opcionales para el cálculo parcial — si no existen, su componente es 0
- Nunca lanza error por datos faltantes — solo por estructura mal configurada (aspectos ≠ 0.90)

### Restricciones de validación
- Suma de `Aspect.weight` en una `GradebookStructure`: exactamente **0.90** (reservando 0.10 para self/peer)
- Suma de `Indicator.weight` en un `Aspect`: exactamente 1.0 (100%)
- Suma de `Activity.weight` en un `Indicator`: exactamente 1.0 (100%)
- Escala de notas: 0.0 a 5.0 (sistema colombiano)
- `PeerEvaluation`: `evaluatorId !== evaluatedId` — nadie se coevalúa a sí mismo

---

## 🗺️ GLOSARIO DE DOMINIO

| Término | Modelo Prisma | Descripción |
|---|---|---|
| **Curso** | `Course` | Asignatura dictada por un TEACHER. Tiene matrícula de estudiantes. |
| **Período** | `Period` | Corte académico dentro de un curso (ej: "Período 1", "Primer bimestre"). |
| **Clase** | `Class` | Presentación interactiva tipo Nearpod. Pertenece a un curso. |
| **Slide** | `Slide` | Diapositiva dentro de una clase. Tiene un tipo y contenido JSON libre. |
| **Estructura de calificación** | `GradebookStructure` | Configuración de cómo se calcula la nota de un curso. Una por curso. |
| **Aspecto** | `Aspect` | Dimensión de evaluación (ej: Proceso, Convivencia). Tiene un peso relativo. |
| **Indicador** | `Indicator` | Sub-categoría dentro de un aspecto (ej: Responsabilidad, Participación). |
| **Actividad** | `Activity` | Tarea/evaluación calificable. Pertenece a un indicador. |
| **Entrada de nota** | `GradeEntry` | Nota de un estudiante en una actividad para un período. |
| **Matrícula** | `Enrollment` | Relación estudiante ↔ curso. |
| **Autoevaluación** | `SelfEvaluation` | Nota que el estudiante se asigna a sí mismo por período. |
| **Coevaluación** | `PeerEvaluation` | Nota que un estudiante asigna a otro compañero. |

---

## 🏛️ PATRÓN DE MÓDULO DE REFERENCIA

Todo módulo nuevo debe seguir exactamente este patrón (basado en CoursesModule):

### Estructura de archivos
```
src/nombre-modulo/
├── nombre-modulo.module.ts
├── nombre-modulo.controller.ts
├── nombre-modulo.service.ts
└── dto/
    ├── create-nombre.dto.ts
    └── update-nombre.dto.ts
```

### Patrón del Service

```typescript
import { Injectable, NotFoundException, ForbiddenException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class NombreService {
  constructor(private readonly prisma: PrismaService) {}

  async findAll(page = 1, limit = 10) {
    const skip = (page - 1) * limit;
    const [items, total] = await Promise.all([
      this.prisma.model.findMany({
        where: { isActive: true },
        select: { id: true, name: true /* nunca password */ },
        skip,
        take: limit,
        orderBy: { createdAt: 'desc' },
      }),
      this.prisma.model.count({ where: { isActive: true } }),
    ]);
    return {
      data: items,
      meta: { total, page, limit, totalPages: Math.ceil(total / limit) },
    };
  }

  async findOne(id: string) {
    const item = await this.prisma.model.findUnique({
      where: { id },
      select: { id: true, name: true },
    });
    if (!item) throw new NotFoundException(`Recurso ${id} no encontrado`);
    return item;
  }

  async remove(id: string) {
    await this.findOne(id); // lanza NotFoundException si no existe
    return this.prisma.model.update({
      where: { id },
      data: { isActive: false }, // soft delete — NUNCA usar delete()
    });
  }
}
```

### Patrón del Controller

```typescript
import { Controller, Get, Post, Patch, Delete, Param, Body, Query, UseGuards, Request } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { RolesGuard } from '../auth/roles.guard';
import { Roles } from '../auth/roles.decorator';

@Controller('nombre')
@UseGuards(JwtAuthGuard)
export class NombreController {
  constructor(private readonly service: NombreService) {}

  @Get()
  findAll(@Query('page') page = '1', @Query('limit') limit = '10') {
    return this.service.findAll(+page, +limit);
  }

  @Post()
  @UseGuards(RolesGuard)
  @Roles('TEACHER', 'ADMIN')
  create(@Body() dto: CreateNombreDto, @Request() req) {
    return this.service.create(dto, req.user.id);
  }
}
```

### Patrón del DTO

```typescript
import { IsString, IsOptional, IsNumber, Min, Max } from 'class-validator';
import { Transform } from 'class-transformer';

export class CreateNombreDto {
  @IsString()
  @Transform(({ value }) => value?.trim())
  name: string;

  @IsOptional()
  @IsString()
  description?: string;
}
```

---

## 🔐 PATRONES DE SEGURIDAD ESTABLECIDOS

```typescript
// Proteger ruta con JWT
@UseGuards(JwtAuthGuard)

// Proteger ruta con JWT + Rol
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'TEACHER')

// Rate limiting en endpoints sensibles
@UseGuards(ThrottlerGuard)

// ThrottlerModule.forRoot (app.module): en @nestjs/throttler v5+, ttl va en MILISEGUNDOS (ej. 60_000 = 60 s)
```

---

## 📐 CONVENCIONES DE CÓDIGO

- **DTOs:** siempre con `class-validator` + `class-transformer`
- **Servicios:** manejar errores con excepciones de NestJS (`NotFoundException`, `ConflictException`, `ForbiddenException`)
- **Soft delete:** NUNCA usar `prisma.model.delete()`. Siempre `update({ data: { isActive: false } })`
- **Paginación:** siempre incluir `meta: { total, page, limit, totalPages }`
- **Selects de Prisma:** siempre usar `select` explícito, NUNCA devolver el campo `password`
- **Imports:** usar rutas relativas, nunca absolutas en `src/`
- **Errores de Prisma:** capturar `P2002` (unique constraint) con `ConflictException`
- **IDs:** son `cuid()` — siempre `String`, nunca `Number`

---

## 🐛 ERRORES CONOCIDOS Y SOLUCIONES

### 1. Prisma 7 — Cannot find module
**Problema:** NestJS compila a `dist/src/` y las rutas relativas al cliente de Prisma se rompen.  
**Solución:** Usar ruta absoluta con `process.cwd()`:
```typescript
const { PrismaClient } = require(
  path.join(process.cwd(), 'prisma/generated/prisma')
);
```

**Nota (24/03/2026):** En este repo el anidamiento en `dist/src/` solía deberse a compilar también `prisma.config.ts` en la raíz junto con `src/` (ver **§9** más abajo). Tras excluir `prisma.config.ts` en `tsconfig.build.json`, el build de Nest emite `dist/main.js` y el árbol plano bajo `dist/`; el servicio usa `import { PrismaClient } from '@prisma/client'` y no hace falta el `require` con `process.cwd()` para el arranque normal. Conserva el snippet anterior si reaparece `Cannot find module` por rutas custom al cliente generado, monorepos o configuraciones distintas.

### 2. JWT_SECRET undefined al arrancar
**Problema:** `JwtStrategy requires a secret or key` al montar AppModule.  
**Solución:** Usar `ConfigModule.forRoot({ isGlobal: true })` y `ConfigService.getOrThrow('JWT_SECRET')`.

### 3. pnpm no reconocido en PowerShell
**Problema:** `pnpm no se reconoce como comando interno`.  
**Solución:** `npm install -g pnpm` y reiniciar terminal.

### 4. PostgreSQL PATH no reconocido
**Problema:** `psql no se reconoce como comando interno`.  
**Solución:** `setx PATH "%PATH%;C:\Program Files\PostgreSQL\18\bin"` y reiniciar terminal.

### 5. Docker Desktop no inicia
**Problema:** `unable to get image` o `dockerDesktopLinuxEngine not found`.  
**Solución:** Abrir Docker Desktop manualmente. Si falla, ejecutar `wsl --update` como administrador.

### 6. PowerShell bloquea scripts
**Problema:** `No se puede cargar el archivo pnpm.ps1 porque la ejecución de scripts está deshabilitada`.  
**Solución:** `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

### 7. Campo no existe en Prisma
**Problema:** `Unknown field X for select statement on model Y`.  
**Solución:** Verificar el schema. Agregar el campo y ejecutar `npx prisma migrate dev`.

### 8. tsconfig moduleResolution nodenext
**Problema:** Conflictos de tipos con `@types/pg` usando `nodenext`.  
**Solución:** Cambiar a `"module": "commonjs"` y `"moduleResolution": "node"` en tsconfig.json.

### 9. Build anidado en `dist/src/` y `start:prod` no encuentra `main.js`
**Problema:** Al compilar, TypeScript emitía `dist/src/main.js` en lugar de `dist/main.js`. El script `start:prod` (`node dist/main`) fallaba. Causa: `prisma.config.ts` en la raíz del repo se incluía en el build junto con `src/`, y el compilador preservaba la jerarquía de carpetas (`dist/` + `dist/src/`).  
**Solución:** Excluir `prisma.config.ts` del build de Nest en `tsconfig.build.json`: en el array `exclude`, añadir `"prisma.config.ts"`. Tras el cambio, la salida queda en `dist/main.js`, `dist/app.module.js`, etc. (sin carpeta `dist/src/` para el código de aplicación).

### 10. ThrottlerModule: `ttl` en milisegundos (@nestjs/throttler v5+)
**Problema:** Con `ttl: 60` el límite era de 60 milisegundos, no 60 segundos; el rate limiting era prácticamente inexistente.  
**Solución:** Usar `ttl` en ms, p. ej. `60_000` para un minuto. Mantener `limit` según la política deseada (p. ej. 10 peticiones por ventana).

### 11. Import de DTO con ruta `../../../src/auth/...` desde `src/users/dto/`
**Problema:** Import como `'../../../src/auth/dto/register.dto'` sale del árbol `src/` y solo cuadraba con el build anidado en `dist/src/`. Con la salida plana en `dist/`, el `require` en runtime podía resolverse mal.  
**Solución:** Usar rutas relativas dentro de `src/`, p. ej. `'../../auth/dto/register.dto'` desde `src/users/dto/update-user.dto.ts`.

### 12. `PORT` con `ConfigService.get` en `main.ts`
**Problema:** `config.get<number>('PORT')` puede tiparse como `number | undefined`; es frágil frente a cambios de validación.  
**Solución:** Usar `config.getOrThrow<number>('PORT')` (alineado con JWT y demás) y reutilizar la variable para `listen` y el `console.log`.

### 13. `users.service`: solo `ADMIN` podía cambiar rol / `isActive`
**Problema:** La condición comprobaba solo `Role.ADMIN`; un usuario `SUPERADMIN` quedaba bloqueado al actualizar `role` o `isActive`.  
**Solución:** Permitir también `Role.SUPERADMIN` en esa misma comprobación de permisos.

---

## 📁 ESTRUCTURA DEL PROYECTO

```
lumina-backend/
├── .env
├── .gitignore
├── docker-compose.yml            # PostgreSQL 16 + Redis 7
├── nest-cli.json
├── package.json
├── prisma.config.ts              # Config de Prisma 7 (excluido del build Nest; ver §9)
├── tsconfig.json
├── tsconfig.build.json           # exclude: incluye prisma.config.ts para salida en dist/
├── prisma/
│   ├── schema.prisma             # 13 modelos definidos
│   ├── migrations/
│   └── generated/prisma/         # Cliente generado (gitignored)
└── src/
    ├── main.ts                   # Bootstrap + ValidationPipe + CORS
    ├── app.module.ts             # ConfigModule + ThrottlerModule + todos los módulos
    ├── prisma/
    │   ├── prisma.module.ts      # Global
    │   └── prisma.service.ts
    ├── auth/
    │   ├── auth.module.ts
    │   ├── auth.controller.ts
    │   ├── auth.service.ts
    │   ├── jwt.strategy.ts
    │   ├── jwt-auth.guard.ts
    │   ├── roles.guard.ts
    │   ├── roles.decorator.ts
    │   └── dto/
    │       ├── register.dto.ts
    │       └── login.dto.ts
    ├── users/
    │   ├── users.module.ts
    │   ├── users.controller.ts
    │   ├── users.service.ts
    │   └── dto/
    │       └── update-user.dto.ts
    ├── courses/
    │   ├── courses.module.ts
    │   ├── courses.controller.ts
    │   ├── courses.service.ts
    │   └── dto/
    │       ├── create-course.dto.ts
    │       ├── update-course.dto.ts
    │       └── enroll-student.dto.ts
    ├── classes/
    │   └── (classes.controller, classes.service, dto/*)
    ├── performance-indicators/
    │   ├── performance-indicators.module.ts
    │   ├── performance-indicators.controller.ts
    │   ├── performance-indicators.service.ts
    │   └── dto/
    │       ├── create-aspect.dto.ts
    │       ├── update-aspect.dto.ts
    │       ├── create-indicator.dto.ts
    │       └── update-indicator.dto.ts
    ├── activities/
    │   ├── activities.module.ts
    │   ├── activities.controller.ts
    │   ├── activities.service.ts
    │   └── dto/
    │       ├── create-activity.dto.ts
    │       └── update-activity.dto.ts
    ├── gradebook/
    │   ├── gradebook.module.ts
    │   ├── gradebook.controller.ts
    │   ├── gradebook.service.ts
    │   └── dto/
    │       ├── create-period.dto.ts
    │       ├── update-period.dto.ts
    │       ├── create-grade-entry.dto.ts
    │       └── update-grade-entry.dto.ts
    ├── grade-calculation/
    │   ├── grade-calculation.module.ts
    │   ├── grade-calculation.controller.ts
    │   ├── grade-calculation.service.ts
    │   └── dto/
    │       └── calculate-grade.dto.ts
    ├── self-evaluation/
    │   ├── self-evaluation.module.ts
    │   ├── self-evaluation.controller.ts
    │   ├── self-evaluation.service.ts
    │   └── dto/
    │       ├── create-self-evaluation.dto.ts
    │       └── update-self-evaluation.dto.ts
    └── peer-evaluation/
        ├── peer-evaluation.module.ts
        ├── peer-evaluation.controller.ts
        ├── peer-evaluation.service.ts
        └── dto/
            ├── create-peer-evaluation.dto.ts
            └── update-peer-evaluation.dto.ts
```

---

## 🗺️ FASES DE DESARROLLO

### Fase 1 — MVP Core ✅ COMPLETA (25/03/2026)
- [x] Auth
- [x] Users
- [x] Courses
- [x] Classes
- [x] Performance Indicators (`PerformanceIndicatorsModule`)
- [x] Gradebook Structure (`GradebookStructure` → `Aspect` → `Indicator` vía API gradebook)
- [x] Activities calificables (`Activity` bajo `Indicator`, `ActivitiesModule`)
- [x] Períodos y notas (`Period`, `GradeEntry`, `GradebookModule`; libro `GET .../grades?periodId=`)
- [x] Grade Calculation (`GradeCalculationModule`) — fórmula multinivel, cálculo parcial, resumen por curso
- [x] Self Evaluation (`SelfEvaluationModule`) — el docente ingresa la nota que el estudiante se asigna
- [x] Peer Evaluation (`PeerEvaluationModule`) — el docente ingresa las notas que los estudiantes se asignan entre sí

### Fase 2 — Funcionalidades interactivas ✅ COMPLETA (25/03/2026)
- [x] Live Sessions (Socket.IO) (`LiveSessionsModule`) — completado 25/03/2026
- [x] Activities H5P (`H5pActivitiesModule`) — completado 25/03/2026
- [x] Gamification (`GamificationModule`) — puntos + badges — completado 25/03/2026
- [x] Class Editor (`ClassEditorModule`) — reorder, duplicar, bulk update, preview — completado 25/03/2026
- [x] Messaging (`MessagingModule`) — canal, DMs, hilos, lectura, soft delete — completado 25/03/2026

### Fase 3 — Analytics y reportes ✅ COMPLETA (25/03/2026)
- [x] Analytics (`AnalyticsModule`) — resumen, progreso, ranking actividades, engagement, export — completado 25/03/2026
- [x] Gradebook Reports (`GradebookReportsModule`) — matriz, distribución, pendientes, export — completado 25/03/2026
- [x] Performance Tracking (`PerformanceTrackingModule`) — evolución, comparativa, ranking, at-risk, breakdown, historial cruzado — completado 25/03/2026

### Fase 4 — Funcionalidades avanzadas ✅ COMPLETA backend (25/03/2026) — Mobile Responsive pendiente (frontend)
- [x] AI Features (`AiFeaturesModule`) — quiz generator, student feedback, class summary, content assistant, evaluate response — completado 25/03/2026
- [x] Collaboration (`CollaborationModule`) — pizarras Fabric.js, notas Tiptap + historial, grupos de trabajo, actividades de grupo — completado 25/03/2026
- [x] Forums (`ForumsModule`) — foros por curso, hilos, respuestas, pin/lock, soft delete — completado 25/03/2026
- [x] Integrations (`IntegrationsModule`) — webhooks salientes, firma HMAC-SHA256, logs paginados, dispatch() exportado — completado 25/03/2026
- [ ] Mobile Responsive (pendiente — tarea de frontend)

---

## 👤 USUARIOS DE PRUEBA

| Email | Password | Rol |
|---|---|---|
| jaime@lumina.edu.co | lumina1234 | TEACHER |
| admin@lumina.edu.co | admin1234 | ADMIN |

---

## 📝 NOTAS IMPORTANTES

1. **Prisma 7** es significativamente diferente a versiones anteriores. La conexión va en `prisma.config.ts`, no en `schema.prisma`.
2. **pnpm** es el gestor de paquetes del proyecto. Nunca usar npm o yarn.
3. **Docker** debe estar corriendo antes de iniciar el servidor.
4. Los warnings de `LF will be replaced by CRLF` en Git son normales en Windows, no son errores.
5. El campo `institution` se agregó al modelo User en la migración `20260321163847_add_institution_to_user`.
6. Cursor realizó mejoras de seguridad al módulo auth: ConfigModule, rate limiting, DTOs estrictos y tests.
7. **24/03/2026:** `tsconfig.build.json` excluye `prisma.config.ts` para que `nest build` genere `dist/main.js` y `pnpm run start:prod` funcione.
8. **24/03/2026:** En `app.module.ts`, `ThrottlerModule`: `ttl` en milisegundos (`60_000` = 60 s), no segundos literales pequeños.
9. **24/03/2026:** `src/users/dto/update-user.dto.ts` importa `Role` desde `../../auth/dto/register.dto` (nunca `../../../src/...`).
10. **24/03/2026:** `src/main.ts` usa `getOrThrow('PORT')` para el puerto de escucha.
11. **24/03/2026:** `src/users/users.service.ts` permite a `SUPERADMIN` además de `ADMIN` cambiar `role` e `isActive` en `update`.
12. **25/03/2026:** Suma de `Aspect.weight` debe ser **0.90** (no 1.0) — los 0.10 restantes son fijos para SelfEval (0.05) y PeerEval (0.05). Validar en `PerformanceIndicatorsModule` y `GradeCalculationModule`.
13. **25/03/2026:** El docente es **siempre** quien ingresa las notas — incluyendo autoevaluación y coevaluación. El estudiante solo tiene acceso de lectura (`GET`) a sus propios registros. Nunca dar permisos de escritura al rol `STUDENT` en ningún módulo de calificación.

---

## 📌 REVISIÓN 24/03/2026 — Cambios realizados en el código (referencia para Cursor)

Resumen de la revisión solicitada al asistente: errores detectados y correcciones ya aplicadas en el repositorio. No sustituye el resto de este archivo; amplía el contexto.

| Área | Qué estaba mal | Qué se hizo |
|------|----------------|-------------|
| **Build / producción** | La compilación dejaba el JS en `dist/src/…`; `package.json` script `start:prod` ejecuta `node dist/main`, que no existía en la ruta esperada. | En `tsconfig.build.json`, `exclude` incluye `"prisma.config.ts"` para que solo se compile el árbol bajo `src/` (salida plana bajo `dist/`). |
| **Rate limiting** | `@nestjs/throttler` v5+: `ttl` está en **ms**. `ttl: 60` ≈ 60 ms, no 1 minuto. | En `app.module.ts`, `ttl: 60_000` (ej. 10 peticiones por minuto con `limit: 10`). |
| **Imports** | `update-user.dto.ts` usaba `'../../../src/auth/dto/register.dto'`, dependiente del layout erróneo de `dist/`. | Import corregido a `'../../auth/dto/register.dto'`. |
| **Bootstrap** | `config.get('PORT')` podía ser `undefined` a nivel de tipos. | `const port = config.getOrThrow<number>('PORT')`; `listen(port)` y log con la misma variable. |
| **Autorización** | En `update`, solo `ADMIN` podía tocar `role` / `isActive`. | Se añadió `SUPERADMIN` a la condición de permiso. |

**Archivos tocados en esa revisión:** `tsconfig.build.json`, `src/app.module.ts`, `src/main.ts`, `src/users/dto/update-user.dto.ts`, `src/users/users.service.ts`.

**Comprobación recomendada tras cambios similares:** `pnpm run build` y verificar que exista `dist/main.js`; revisar que `ThrottlerModule` use `ttl` en milisegundos según la versión del paquete.

---

## 📌 REVISIÓN 25/03/2026 — Fase 1 completada

### Módulos nuevos agregados

| Módulo | Ruta | Endpoints |
|--------|------|-----------|
| `GradeCalculationModule` | `src/grade-calculation/` | `GET .../grade-calculation`, `GET .../grade-calculation/full`, `GET .../grade-calculation/students/:userId` |
| `SelfEvaluationModule` | `src/self-evaluation/` | `POST/GET/PATCH/DELETE .../self-evaluations` y `.../self-evaluations/students/:userId` |
| `PeerEvaluationModule` | `src/peer-evaluation/` | `POST/GET .../peer-evaluations`, `GET .../peer-evaluations/students/:evaluatedId`, `PATCH/DELETE .../peer-evaluations/:id` |

### Decisiones de dominio confirmadas

| Decisión | Detalle |
|----------|---------|
| **Quién ingresa notas** | Siempre el docente — incluyendo autoevaluación y coevaluación. El estudiante nunca escribe. |
| **Peso máximo de aspectos** | 0.90 (no 1.0). SelfEval (0.05) + PeerEval (0.05) = 0.10 reservado. Total siempre = 1.0 → máx 5.0 |
| **Cálculo parcial** | `GradeCalculationModule` calcula con lo disponible y devuelve `isComplete: false` si faltan entradas |
| **Restricción peer** | `evaluatorId !== evaluatedId` — nadie se coevalúa a sí mismo, aunque sea el docente quien lo ingrese |

### Correcciones aplicadas y verificadas en pruebas (25/03/2026)

| Archivo | Cambio aplicado | Verificado |
|---------|-----------------|-----------|
| `src/performance-indicators/performance-indicators.service.ts` | Límite de suma de aspectos cambiado de `1.0` a `0.90` | ✅ Devuelve 400 al intentar superar 0.90 |
| `src/grade-calculation/grade-calculation.service.ts` | Validación explícita suma aspectos = 0.90 antes de calcular | ✅ Cálculo multinivel y nota final correctos |

### Commit de cierre Fase 1

```
[main 5a4f769] feat: complete Phase 1 - GradeCalculation, SelfEvaluation, PeerEvaluation modules
17 files changed, 1193 insertions(+), 49 deletions(-)
```

### Resultados de pruebas en Postman (25/03/2026)

| Prueba | Resultado |
|--------|-----------|
| Login + JWT | ✅ |
| Validación suma aspectos > 0.90 devuelve 400 | ✅ |
| Cálculo multinivel parcial con `isComplete: false` | ✅ |
| Autoevaluación ingresada por docente | ✅ |
| Coevaluación rechaza evaluador no matriculado | ✅ |
| Nota final con autoevaluación: 3.15 + (4.0 × 0.05) = 3.35 | ✅ |


---

## 📌 REVISIÓN 25/03/2026 — Refactor autorización, LiveSessions y tipado Prisma

### 1. `CourseAuthorizationService` (nuevo servicio global)

**Archivo:** `src/common/course-authorization.service.ts`  
**Módulo:** `src/common/common.module.ts` — decorado con `@Global()`, exporta el servicio.  
**Registro:** `CommonModule` importado en `app.module.ts` junto a `PrismaModule`.

Centraliza toda la lógica de acceso a cursos que antes se duplicaba (~40 líneas) en 6 módulos:

| Método | Descripción |
|--------|-------------|
| `assertCourseExists(courseId)` | Lanza 404 si el curso no existe |
| `verifyCourseReadAccess(courseId, userId, role)` | ADMIN/SUPERADMIN pasan siempre; TEACHER solo si es titular; STUDENT solo si está matriculado |
| `assertStaffCanManageCourse(courseId, userId, role, scope)` | Para mutaciones; `scope`: `'grades' \| 'gradebook' \| 'activities' \| 'selfEvaluations' \| 'peerEvaluations' \| 'liveSessions'` |
| `isStaffForCourse(courseId, userId, role)` | Retorna `boolean` sin lanzar excepción (usado en LiveSessions para distinguir host) |

**Servicios refactorizados** (eliminaron métodos privados equivalentes):
- `gradebook.service.ts` → scope `'grades'`
- `performance-indicators.service.ts` → scope `'gradebook'`
- `activities.service.ts` → scope `'activities'`
- `grade-calculation.service.ts` → solo `verifyCourseReadAccess`
- `self-evaluation.service.ts` → scope `'selfEvaluations'`
- `peer-evaluation.service.ts` → scope `'peerEvaluations'`
- `classes.service.ts` → solo `verifyCourseReadAccess`

---

### 2. Fix de tests (4 suites que fallaban)

| Spec | Fix aplicado |
|------|-------------|
| `courses.service.spec.ts` | `PrismaService` con `useValue` (mock de `course` y `enrollment`) |
| `users.service.spec.ts` | Mock de `user` |
| `courses.controller.spec.ts` | `CoursesService` con `useValue: {}` |
| `users.controller.spec.ts` | `UsersService` con `useValue: {}` |

Resultado: **8 suites passed**, `pnpm run build` OK.

---

### 3. `LiveSessionsModule` — Fase 2 (implementado)

**Archivos:** `src/live-sessions/`  
**Namespace Socket.IO:** `/live`  
**Dependencias instaladas:** `@nestjs/websockets`, `@nestjs/platform-socket.io`, `socket.io`

#### Eventos

| Evento (cliente → servidor) | Rol | Efecto |
|-----------------------------|-----|--------|
| `session:start` `{ classId }` | Anfitrión | PUBLISHED → LIVE; limpia estado de slides; broadcast `session:started`. Si ya era LIVE, solo entra a la sala sin borrar estado. |
| `session:end` `{ classId }` | Anfitrión | LIVE → PUBLISHED; borra estado; broadcast `session:ended`; expulsa a todos de la sala. |
| `join` `{ classId }` | Cualquiera con acceso | Entra en sala; ack `{ ok, classId, room, currentSlide, isHost }` |
| `slide:state` `{ classId }` | Igual que join | ack `{ ok, currentSlide }` sin hacer join |
| `slide:sync` `{ classId, slideId }` | Anfitrión + LIVE | Guarda estado en memoria; emite `slide:current` a los demás |
| `leave` `{ classId }` | — | Sale de la sala |

#### Reglas de negocio
- Estado de diapositiva **en memoria** por `classId` (`slideId`, `order`, `updatedBy`, `updatedAt`). En múltiples réplicas se necesitaría Redis.
- Anfitrión puede hacer `join`/`session:start` con clase **PUBLISHED** o **LIVE**.
- Participantes solo pueden `join` cuando la clase está **LIVE**.
- `slide:sync` solo permitido con clase **LIVE**.
- Conexión: `auth: { token: '<jwt>' }` o header `Authorization: Bearer <jwt>`.
- CORS del gateway usa `FRONTEND_URL` (igual que `main.ts`).

#### Flujo recomendado (frontend)
1. Docente: conectar con JWT → `session:start` → `slide:sync` al cambiar diapositiva.
2. Estudiantes: esperar evento `session:started` (o polling HTTP) → `join` → usar `currentSlide` del ack.
3. Docente al terminar: `session:end`.

---

### 4. Tipado limpio de Prisma (sin `as unknown as PrismaClient`)

**Problema:** Con Prisma 7 + `PrismaPg` adapter, `PrismaService` no solapaba bien con el tipo generado `PrismaClient`, causando 6 errores de inferencia en el IDE.

**Solución aplicada:**

`src/prisma/prisma.module.ts` registra el mismo singleton dos veces:
```typescript
providers: [
  PrismaService,
  { provide: PrismaClient, useExisting: PrismaService },
],
exports: [PrismaService, PrismaClient],
```

En servicios donde el IDE se queje de delegates no tipados, inyectar `PrismaClient` en lugar de `PrismaService`:
```typescript
constructor(private readonly prisma: PrismaClient) {}
```

La instancia es la misma en runtime. `PrismaService` sigue siendo válido en el resto de servicios — no hace falta migrar todo de golpe.

**Aplicado en:** `src/live-sessions/live-sessions.service.ts`

---

### 5. Estructura del proyecto actualizada
```
src/
├── common/
│   ├── common.module.ts              # @Global(), exporta CourseAuthorizationService
│   └── course-authorization.service.ts
└── live-sessions/
    ├── live-sessions.module.ts       # importa AuthModule (JWT)
    ├── live-sessions.gateway.ts      # namespace /live
    ├── live-sessions.service.ts      # lógica + estado en memoria
    └── dto/
        ├── join-live.dto.ts
        └── slide-sync.dto.ts
```

---

### 6. Fase 2 — Estado actualizado

- [x] Live Sessions (Socket.IO) — `LiveSessionsModule` implementado y registrado en `app.module.ts`
- [x] Activities (H5P) — `H5pActivitiesModule` completado 25/03/2026
- [x] Gamification — `GamificationModule` completado 25/03/2026
- [x] Class Editor — `ClassEditorModule` completado 25/03/2026
- [x] Messaging — `MessagingModule` completado 25/03/2026

---

### 7. Commit de cierre de esta sesión
```
feat: CourseAuthorizationService, LiveSessionsModule completo, fix Prisma typing; docs .cursorrules
```

**Archivos en el commit:**
- `src/common/` (nuevo)
- `src/live-sessions/` (nuevo)
- `src/prisma/prisma.module.ts`, `prisma.service.ts`
- `src/activities/activities.service.ts`
- `src/app.module.ts`
- `src/classes/classes.service.ts`
- `src/grade-calculation/grade-calculation.controller.ts`, `grade-calculation.service.ts`
- `src/gradebook/gradebook.service.ts`
- `src/peer-evaluation/peer-evaluation.controller.ts`, `peer-evaluation.service.ts`
- `src/performance-indicators/performance-indicators.service.ts`
- `src/self-evaluation/self-evaluation.service.ts`
- `src/courses/courses.controller.spec.ts`, `courses.service.spec.ts`
- `src/users/users.controller.spec.ts`, `users.service.spec.ts`
- `package.json`, `pnpm-lock.yaml`
- `.cursorrules`

---

## 📌 REVISIÓN 26/03/2026 — Correcciones detectadas en pruebas Postman

### Cambios aplicados

| Módulo | Área | Cambio |
|--------|------|--------|
| **GamificationModule** | `AssignPointsDto` | Agregado campo `reason?: string` con `@IsOptional()` y `@IsString()` |
| **GamificationModule** | `StudentPoints` (schema) | Agregado campo `reason String?` |
| **GamificationModule** | `gamification.service.ts` | `upsertPoints`: `create` y `update` incluyen `reason: dto.reason` |
| **MessagingModule** | `SendMessageDto` / `SendDirectMessageDto` | Renombrado campo `body` → `content` para consistencia con el resto de la plataforma |
| **MessagingModule** | `Message` (schema) | Renombrado campo `body` → `content` |
| **MessagingModule** | `messaging.service.ts` | `MESSAGE_SELECT`, `sendMessage`, `sendDirectMessage`, `deleteMessage`: usan `content` en lugar de `body` |

### Migración aplicada

```
20260326120000_rename_body_to_content_add_reason
```

SQL ejecutado (manual para preservar datos existentes en `messages`):
```sql
ALTER TABLE "messages" RENAME COLUMN "body" TO "content";
ALTER TABLE "student_points" ADD COLUMN "reason" TEXT;
```

### Resultado

- `pnpm run build` ✅
- `pnpm test` ✅ (8 suites, 12 tests)

### Archivos modificados

- `src/gamification/dto/assign-points.dto.ts`
- `src/gamification/gamification.service.ts`
- `src/messaging/dto/send-message.dto.ts`
- `src/messaging/dto/send-direct-message.dto.ts`
- `src/messaging/messaging.service.ts`
- `prisma/schema.prisma`
- `prisma/migrations/20260326120000_rename_body_to_content_add_reason/migration.sql`
