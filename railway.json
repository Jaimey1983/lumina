{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @lumina/types build && pnpm --filter @lumina/curriculum-data build && pnpm --filter @lumina/scoring build && pnpm --filter lumina-backend exec prisma generate && pnpm --filter lumina-backend build",
    "watchPatterns": [
      "lumina-backend/**",
      "packages/types/**",
      "packages/curriculum-data/**",
      "packages/scoring/**",
      "pnpm-lock.yaml",
      "pnpm-workspace.yaml",
      "package.json",
      "railway.json"
    ]
  },
  "deploy": {
    "preDeployCommand": [
      "echo '###### INICIO_PRISMA_MIGRATE ######' && pnpm --filter lumina-backend exec prisma migrate deploy; echo \"###### FIN_PRISMA_MIGRATE exit=$? ######\""
    ],
    "startCommand": "pnpm --filter lumina-backend start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
