{
  "name": "@lumina/scoring",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Paquete interno dual (E6.1/E6.2). TS/ESM (frontend con transpilePackages, element-kit) se consumen desde `src/` vía `exports.types`/`import`/`default`. `main` + `types` de nivel superior → build CJS/`.d.ts` en `dist/` para lumina-backend (`moduleResolution: node` clásico, no lee `exports`). `exports` tiene prioridad para quien lo soporte.",
  "main": "./dist/cjs/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "require": "./dist/cjs/index.js",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./fixtures": "./src/activity-scoring.fixtures.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.cjs.json && node ./scripts/finish-cjs.mjs",
    "test": "tsc --noEmit && vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
