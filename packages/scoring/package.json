{
  "name": "@lumina/scoring",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Paquete interno dual (E6.1). TS/ESM (frontend con transpilePackages, element-kit) se consumen desde `src/` vía `types`/`import`/`default`. `require` → build CJS en `dist/cjs/` para lumina-backend (CommonJS/jest, E6.2+). `dist/index.js` (ESM) se mantiene para el .d.ts publicable a futuro.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "require": "./dist/cjs/index.js",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.cjs.json && node ./scripts/finish-cjs.mjs",
    "test": "tsc --noEmit && vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
