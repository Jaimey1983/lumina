{
  "name": "@lumina/scoring",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Paquete interno: se consume desde fuente (TS). Los consumidores del monorepo (lumina-frontend con transpilePackages) no necesitan `dist/`. `build` sigue existiendo para el job `packages` de CI y para el .d.ts publicable a futuro.",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc --noEmit && vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
