import type { EditorPersistHost } from "@lumina/editor-shared/editor-persist-host";

/** Config común del panel de primitivos (P3 persistencia). */
export type PrimitivePanelConfig = {
  persistHost?: EditorPersistHost;
};
